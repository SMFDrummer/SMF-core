package com.smf.core.blocks

import net.minecraft.world.level.block.state.BlockBehaviour

/**
 * Tempered glass used by SMF multiblock machines (hideable while a structure is assembled).
 */
class TemperedGlassBlock(properties: BlockBehaviour.Properties) : SMFHideableBlock(properties)